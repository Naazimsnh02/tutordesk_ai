# Custom frontend (Off-Brand badge)

Phase 6: replace the default Gradio layout with a custom frontend served via
`gr.Server` to claim the **Off-Brand** award. Mobile-first, teacher-focused, one-tap flows.

Until then, `app.py` uses standard `gr.Blocks`.
