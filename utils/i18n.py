"""UI string localization (labels/buttons). Content localization lives in models/aya.py."""
from __future__ import annotations

from config import CONFIG

# TODO Phase 5: minimal UI label dictionary per language in CONFIG.languages.
SUPPORTED = CONFIG.languages
